name: CI/CD Pipeline

on:
  push:
    branches: [ main, develop, kiet,DUCTOAN,babyfat,phat]
  pull_request:
    branches: [ main, develop ]

env:
  NODE_VERSION: '16.x'
  MONGO_URL: mongodb://localhost:27017/Clothes_Test

jobs:
  # ============= SERVER TESTS =============
  server-tests:
    name: Server Unit & Integration Tests
    runs-on: ubuntu-latest
    timeout-minutes: 20

    services:
      mongodb:
        image: mongo:5
        ports:
          - 27017:27017
        options: >-
          --health-cmd "mongosh --eval 'db.runCommand({ ping: 1 })'"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js (cache npm)
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'npm'
          cache-dependency-path: server_app/package-lock.json

      - name: Install server dependencies (faster)
        working-directory: ./server_app
        run: npm ci --no-audit --no-fund --prefer-offline

      - name: Run server tests (unit + integration) with coverage (non-blocking)
        working-directory: ./server_app
        continue-on-error: true
        run: |
          npx jest \
            --ci \
            --coverage \
            --coverageReporters=lcov,text-summary \
            --maxWorkers=100% \
            --passWithNoTests \
            --coverageThreshold='{"global":{"statements":0,"branches":0,"lines":0,"functions":0}}'
        env:
          MONGO_URL: ${{ env.MONGO_URL }}
          JWT_SECRET: test_secret_key_for_ci
          NODE_ENV: test

      - name: Upload server coverage
        if: always()
        uses: codecov/codecov-action@v4
        with:
          files: ./server_app/coverage/lcov.info
          flags: server
          name: server-coverage
          fail_ci_if_error: false
          verbose: false

  # ============= CLIENT TESTS =============
  client-tests:
    name: Client Unit & Integration Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install client dependencies
        working-directory: ./client_app
        run: |
          npm ci --no-audit --no-fund || npm install --no-audit --no-fund

      - name: Run client tests
        working-directory: ./client_app
        continue-on-error: true
        run: npm run test:ci
        env:
          REACT_APP_API_URL: http://localhost:8000/

      - name: Upload client coverage
        if: always()
        uses: codecov/codecov-action@v4
        with:
          files: ./client_app/coverage/lcov.info
          flags: client
          name: client-coverage
          fail_ci_if_error: false
          verbose: false

  # ============= ADMIN TESTS =============
  admin-tests:
    name: Admin Unit & Integration Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Install admin dependencies
        working-directory: ./admin_app
        run: |
          npm ci --no-audit --no-fund || npm install --no-audit --no-fund

      - name: Run admin tests
        working-directory: ./admin_app
        continue-on-error: true
        run: npm run test:ci
        env:
          REACT_APP_API_URL: http://localhost:8000/

      - name: Upload admin coverage
        if: always()
        uses: codecov/codecov-action@v4
        with:
          files: ./admin_app/coverage/lcov.info
          flags: admin
          name: admin-coverage
          fail_ci_if_error: false
          verbose: false

  # ============= LINTING & CODE QUALITY =============
  lint:
    name: Code Quality & Linting
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Lint server code
        working-directory: ./server_app
        run: |
          npm ci --no-audit --no-fund || npm install --no-audit --no-fund
          npm run lint || true

      - name: Lint client code
        working-directory: ./client_app
        run: |
          npm ci --no-audit --no-fund || npm install --no-audit --no-fund
          echo "Client app uses react-scripts linting built-in"

      - name: Lint admin code
        working-directory: ./admin_app
        run: |
          npm ci --no-audit --no-fund || npm install --no-audit --no-fund
          echo "Admin app uses react-scripts linting built-in"

  # ============= BUILD & DOCKER (TỐI ƯU SONG SONG) =============
  build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    needs: [server-tests, client-tests, admin-tests]
    if: github.event_name != 'pull_request'
    permissions:
      contents: read
      packages: write
    
    strategy:
      matrix:
        app: [server, client, admin]

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ghcr.io/${{ github.repository_owner }}/sgu-${{ matrix.app }}
          tags: |
            type=sha,prefix={{branch}}-
            type=ref,event=branch
            type=raw,value=latest,enable={{is_default_branch}}
      
      - name: Build and push image
        uses: docker/build-push-action@v5
        with:
          context: ./${{ matrix.app }}_app
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha,scope=${{ matrix.app }}
          cache-to: type=gha,mode=max,scope=${{ matrix.app }}

  # ============= DEPLOY =============
  deploy:
    name: Deploy to Production
    runs-on: ubuntu-latest
    needs: [build]
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy to server
        run: |
          echo "Deployment step - configure based on your hosting provider"
          # Add your deployment commands here
          # Example: SSH to server and pull latest images
